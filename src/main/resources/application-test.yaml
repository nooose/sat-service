spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: create

external:
  frontend:
    url: http://localhost:3000
